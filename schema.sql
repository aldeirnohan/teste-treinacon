CREATE TABLE "user" (id SERIAL PRIMARY KEY , name varchar(100) NOT NULL, email varchar(100) NOT NULL, password varchar(100) NOT NULL);
INSERT INTO "user" (name, email, password) VALUES ('admin', 'admin@admin.com', '');